meta {
  name: 08 - Data-Driven: MatchScore (CSV)
  seq: 8
}

docs {
  Run this folder with the Bruno runner and attach `data/data-driven-matchscore.csv`.
  The Id per iteration is taken from the CSV column `iterationId` so re-runs are
  safe (and idempotent when identical).
}
