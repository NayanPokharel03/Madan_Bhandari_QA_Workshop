meta {
  name: 07 - Buggy endpoints (documenting defects)
  seq: 7
}

docs {
  These tests assert the CORRECT expected behavior. They intentionally FAIL
  against the current buggy implementations so the defects are visible in the
  run report.
}
